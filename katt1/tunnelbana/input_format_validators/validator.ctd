INT(2, 100000, n) SPACE INT(0, 100000, m) SPACE INT(0, 100000, k) NEWLINE
REPI(i, n-1)
	INT(1, n, u[i]) SPACE INT(1, n, v[i]) NEWLINE
	ASSERT(u[i] != v[i])
END
IF(m != 0)
	ASSERT(UNIQUE(u, v))
END
REPI(i, m)
	INT(1, n, a[i]) SPACE INT(1, n, b[i]) NEWLINE
	ASSERT(a[i] != b[i])
END
EOF
